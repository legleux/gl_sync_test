name: Build matrix mtest

on:
  push:

env:
  REGISTRY_IMAGE: legleux/test_image

jobs:
  build:
    strategy:
      matrix:
        platform: [ {platform: "linux/amd64", runner: "one"}, {platform: "linux/arm64", runner: "two"} ]

    runs-on: "${{ fromJSON('[ubuntu-latest, ubuntu-22.04])' }}"

    steps:
      - name: Run it
        id: tag_name
        shell: bash
        run: |
          echo "I'm to run on ${{ matrix.platform.runner }} with ${{ matrix.platform.platform }}"
